{
  "name": "priyadharshini-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website for Priyadharshini — Graphic Designer",
  "private": true,
  "scripts": {
    "scan": "node scan-assets.js",
    "dev": "npm run scan && vite",
    "build": "npm run scan && vite build",
    "preview": "vite preview",
    "start": "npm run dev",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
